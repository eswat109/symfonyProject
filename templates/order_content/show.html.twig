{% extends 'base.html.twig' %}

{% block title %}OrderContent{% endblock %}

{% block body %}
    <h1>OrderContent</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ order_content.id }}</td>
            </tr>
            <tr>
                <th>Amount</th>
                <td>{{ order_content.amount }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('order_content_index') }}">back to list</a>

    <a href="{{ path('order_content_edit', {'id': order_content.id}) }}">edit</a>

    {{ include('order_content/_delete_form.html.twig') }}
{% endblock %}
